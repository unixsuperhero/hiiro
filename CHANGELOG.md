Done. CHANGELOG.md updated with v0.1.275 entry at the top, dated 2026-03-24, with the `h-pr sync` subcommand addition.

## 2026-03-24 — h-app: fd, rg, vim subcommands

Added `h app fd <app> [args...]`, `h app rg <app> [args...]`, and `h app vim <app> [args...]` subcommands. Each resolves the app's absolute path and execs the tool with the given args and path appended.
